[Unit]
Description=dvs-mapper collects device monitoring measurements and converts them to connectivity provider format.
After=syslog.target network.target mosquitto.service

[Service]
User=dvs-mapper
ExecStart=/usr/bin/dvs_mapper
Restart=on-failure
RestartPreventExitStatus=255

[Install]
WantedBy=multi-user.target
